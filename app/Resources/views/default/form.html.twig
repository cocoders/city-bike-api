{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {% for station in stations %}
    {{ station.name }}
{% endfor %}
{% endblock %}

